# Example Android App

Use this repository / project as a template for building this app

Options for Architectures & Variants, current default is 1 each to reduce build times and apk sizes.

If you plan on using this as an external library set this to off inside the `build.gradle`
